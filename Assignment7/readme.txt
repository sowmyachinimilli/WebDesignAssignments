Name: Satya Sri Sowmya Chinimilli
NUID: 002102974

Designed Baskin Robbins Website using different SCSS features.

Grid Layout:
Grid was designed for two-dimensional layout - rows, and columns at the same time.
In this website grid has been implemented to display the icecream products in a grid layout.

Flexbox Layout:
Flexbox was designed for layout in one dimension.
Few places where flexbox has been used is for elements in about header section and About, history sections to display
side by side.

Variables:
Variables are used to store a value in a name and use the name everywhere instead of bringing the value everytime.
Used the variables for storing colors.

Nesting:
Sass will let you nest your CSS selectors in a way that follows the same visual hierarchy of your HTML. 
Nesting has been implemented at several locations for different elements in the body.

Functions: 
Functions allow you to define complex operations on SassScript values that you can re-use throughout your stylesheet.
Implemented function to change the text color based on the background color. 

Mixins:
Mixins allow you to define styles that can be re-used throughout your stylesheet.
Used mixins to change the background color of the website and also change the text color accordingly.

Interpolation:
Interpolation can be used almost anywhere in a Sass stylesheet to embed the result of a SassScript expression into a chunk of CSS.
Just wraping an expression in #{} in any place.
Interpolation has been used to pass the image names instead of entire url everytime.

Placeholder Selectors: 
It looks and acts a lot like a class selector, but it starts with a % and it's not included in the CSS output.
Used a set a constant font-size overall the website.

Custom Properties: 
The custom properties includes color e.t.c
